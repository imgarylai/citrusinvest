# build output
dist/
.astro/

# deps
node_modules/

# WASM playground artifacts — built by CI (scripts/build-*-wasm.sh) into here.
# The committed data/sample.json is deterministic and kept.
public/wasm/
